'use strict';
(function () {
    angular.module('habit').factory('habitService', function () {
        //This is a singleton that contains all the data for habitService.
        var service = {
        };

        return service;
    });
})();
